// 150+ interview questions across all resume topics
export const QBANK = [
  // ----- GOLANG (25) -----
  { t: "Go", d: "easy", q: "What is a goroutine and how is it different from an OS thread?", a: "A goroutine is a lightweight thread managed by the Go runtime. It starts with about 2KB stack vs ~1MB for an OS thread, and the runtime multiplexes goroutines onto OS threads using the GMP scheduler. You can run hundreds of thousands of goroutines on a single machine." },
  { t: "Go", d: "easy", q: "How do channels work in Go?", a: "Channels are typed conduits for communication between goroutines. Sending: ch <- value. Receiving: v := <-ch. Unbuffered channels block until both sides are ready; buffered channels block when full/empty. They are the primary synchronization mechanism in Go and are usually preferred over shared memory with locks." },
  { t: "Go", d: "med", q: "Explain Go interfaces and how they differ from Java/C# interfaces", a: "Go interfaces are satisfied implicitly — there is no 'implements' keyword. Any type that has the required methods automatically satisfies the interface. This enables duck typing and makes mocking easy. The empty interface (any) holds any value. Interfaces are usually small (1–3 methods) — Rob Pike: 'the bigger the interface, the weaker the abstraction.'" },
  { t: "Go", d: "med", q: "What is context.Context and when do you use it?", a: "context.Context carries deadlines, cancellation signals, and request-scoped values across API boundaries. Pass ctx as the first argument to functions that do I/O. When the parent context is cancelled (request closed, timeout), all downstream operations can also cancel — preventing wasted work. Always derive children with WithCancel, WithTimeout, WithDeadline, or WithValue." },
  { t: "Go", d: "med", q: "How does defer work in Go?", a: "defer schedules a function call to run when the surrounding function returns. Multiple defers run in LIFO order. Use for cleanup: defer file.Close(), defer mu.Unlock(). Gotcha: deferred function args are evaluated immediately, but the function runs at return. Also: avoid deferring in tight loops — defers accumulate until function exit." },
  { t: "Go", d: "hard", q: "Explain the GMP scheduler model", a: "G = goroutine, M = OS thread (machine), P = logical processor (context). GOMAXPROCS sets the number of Ps. Each P has a local run queue; M binds to a P to execute Gs. When a G blocks on syscall, the M detaches and the P picks up another M. Work stealing balances queues. This gives Go cheap concurrency without per-goroutine OS threads." },
  { t: "Go", d: "med", q: "What is a nil interface value vs a nil pointer wrapped in an interface?", a: "An interface in Go has two fields: type and value. A nil interface has both nil. An interface holding a nil pointer has type set but value nil — it is NOT == nil. This is a classic bug: returning a typed nil from a function that returns an interface gives the caller a non-nil interface that fails nil checks." },
  { t: "Go", d: "med", q: "How do you handle errors idiomatically in Go?", a: "Functions return (T, error). Caller checks err != nil immediately. Wrap with context using fmt.Errorf('loading user %s: %w', id, err) — the %w verb keeps the original error for errors.Is and errors.As inspection. Define sentinel errors (var ErrNotFound = errors.New('not found')) or typed errors for richer handling." },
  { t: "Go", d: "med", q: "What is the difference between sync.Mutex and sync.RWMutex?", a: "Mutex allows one goroutine at a time. RWMutex allows many readers OR one writer. Use RWMutex when reads vastly outnumber writes. Always defer mu.Unlock() right after Lock(). Be careful of copying mutexes — they become useless. Use go vet -copylocks to catch this." },
  { t: "Go", d: "hard", q: "How does the Go garbage collector work?", a: "Go uses a concurrent tri-color mark-and-sweep collector with very short stop-the-world pauses (sub-millisecond for most heaps). It runs concurrently with the program. Allocations that escape the stack go to the heap. Use sync.Pool to reuse objects and reduce GC pressure on hot paths. Tune with GOGC (default 100 means GC triggers when heap doubles)." },
  { t: "Go", d: "easy", q: "What is the zero value of various types in Go?", a: "Numbers: 0. Strings: ''. Bool: false. Pointers, interfaces, slices, maps, channels, functions: nil. Structs: each field at its zero. This is why Go has no NullPointerException — but you do need to nil-check pointers and check map/slice length before access." },
  { t: "Go", d: "med", q: "Slice vs Array — what is the difference?", a: "Array has fixed size (part of its type: [3]int and [4]int are different types). Slice is a header (pointer, length, capacity) referencing a backing array. Slices are passed cheaply by value (just the header). Appending past capacity allocates a new backing array." },
  { t: "Go", d: "med", q: "What does the select statement do?", a: "select waits on multiple channel operations and runs whichever is ready first. If multiple are ready, one is chosen randomly. Add a default case for non-blocking behavior. Common pattern: select with time.After for timeouts." },
  { t: "Go", d: "hard", q: "What is the loop variable capture issue?", a: "Pre-Go 1.22, the loop variable was a single variable shared across all iterations. Closures or goroutines launched in the loop all saw the final value. Fix: pass as parameter or shadow inside the loop. Go 1.22 changed this — each iteration gets a fresh variable." },
  { t: "Go", d: "med", q: "How do you prevent goroutine leaks?", a: "Every goroutine must have a clear exit path. Common leaks: forgetting to close channels, blocking on channels with no sender, infinite loops without context cancellation. Use context.WithCancel for cleanup. Use go test with -race and inspect goroutine count in production with pprof." },
  { t: "Go", d: "med", q: "What does iota do?", a: "iota is the index inside a const block, starting at 0 and incrementing by 1 for each ConstSpec. Used to define enums: const (Red = iota; Green; Blue) gives 0, 1, 2. Reset with each const block. You can express bitmasks: 1 << iota." },
  { t: "Go", d: "easy", q: "How do you read JSON in Go?", a: "Define a struct with json: tags, then json.Unmarshal([]byte, &v) or json.NewDecoder(r).Decode(&v). For unknown shapes, use map[string]interface{} or json.RawMessage. For streaming large bodies, use Decoder." },
  { t: "Go", d: "hard", q: "Explain GoFiber vs net/http", a: "Fiber is built on fasthttp, not net/http. fasthttp avoids per-request allocations using object pools — faster but with caveats: not 100% HTTP/2 compliant, Request/Response objects are reused (mind reference lifetimes), and most net/http middleware does not work directly. Trade speed for ecosystem compatibility." },
  { t: "Go", d: "med", q: "What is the difference between make and new?", a: "new(T) allocates zeroed memory and returns a pointer *T. make(T, args) initializes slices, maps, and channels — these need internal structures set up. So: ptr := new(int); s := make([]int, 10)." },
  { t: "Go", d: "med", q: "What is a struct tag?", a: "A backtick-quoted string after a struct field that provides metadata to libraries via reflection. Common: json:'name', db:'col', validate:'required'. Multiple tags space-separated. Used by encoding/json, GORM, validator, etc." },
  { t: "Go", d: "hard", q: "How do generics work in Go?", a: "Added in Go 1.18. Use type parameters with constraints: func Map[T any, U any](s []T, f func(T) U) []U. Constraints define the operations allowed: comparable, constraints.Ordered, or custom interfaces with type sets. Use sparingly — Go style still prefers interfaces for most polymorphism." },
  { t: "Go", d: "med", q: "How would you implement a graceful shutdown for a Fiber server?", a: "Listen for SIGINT/SIGTERM, call app.ShutdownWithTimeout(30 * time.Second), drain in-flight requests, close DB connections, flush logs, then exit. Use a sync.WaitGroup for background workers. Critical for zero-downtime deploys." },
  { t: "Go", d: "hard", q: "What is the rationale behind Go choosing composition over inheritance?", a: "Go embeds types instead of inheriting. struct Foo { Logger; ... } makes Foo have all Logger methods. This avoids the fragile-base-class problem, ambiguity in multiple inheritance, and tightly coupled hierarchies. You combine behaviors instead of forming a single chain." },
  { t: "Go", d: "med", q: "When would you use sync.WaitGroup vs errgroup?", a: "WaitGroup waits for N goroutines. errgroup (from x/sync) does the same but cancels all if any errors. Use errgroup when goroutines share a context and you want fail-fast semantics. WaitGroup is fine for fire-and-forget batches where failures are isolated." },
  { t: "Go", d: "hard", q: "How do you profile a Go service in production?", a: "Import _ 'net/http/pprof' to expose /debug/pprof/* on your server. Capture profiles: go tool pprof -http=:8080 https://prod/debug/pprof/heap. Profiles: cpu, heap, goroutine, mutex, block. Continuous profiling tools: Pyroscope, Datadog. Always sample, not 100%, in prod." },
  // ----- NODE.JS (15) -----
  { t: "Node", d: "easy", q: "Explain the Node.js event loop", a: "Single-threaded loop with phases: timers (setTimeout/setInterval), pending callbacks, idle/prepare, poll (I/O), check (setImmediate), close callbacks. process.nextTick runs before any phase. Microtasks (Promises) run between phases. Long-running synchronous code blocks the loop." },
  { t: "Node", d: "med", q: "process.nextTick vs setImmediate vs setTimeout(0)", a: "nextTick runs immediately after the current operation, before any I/O. setImmediate runs in the check phase, after I/O callbacks. setTimeout(0) is throttled to ~1ms minimum and runs in the timers phase. Order: sync code → nextTick → microtasks → next phase." },
  { t: "Node", d: "med", q: "How do you handle CPU-intensive work in Node?", a: "Offload to worker_threads (true threads in V8 instances), child_process for separate processes, or external services. Never run heavy CPU in the main event loop — it stalls all requests. For CPU-bound microservices, Go is often a better fit." },
  { t: "Node", d: "easy", q: "What are Promises? Promise.all vs Promise.allSettled", a: "Promise is a placeholder for an async result with states: pending, fulfilled, rejected. Promise.all([..]) rejects fast on any failure. allSettled returns array of {status, value/reason} for every input — useful when you want all results regardless of failures." },
  { t: "Node", d: "med", q: "Explain Streams in Node", a: "Streams process data in chunks: Readable (read from source), Writable (write to sink), Duplex (both), Transform (modify). Use pipe() to chain. Support backpressure — slow consumer pauses the producer. Critical for large files and proxies." },
  { t: "Node", d: "med", q: "What is NestJS dependency injection?", a: "Nest uses a DI container. Providers (@Injectable() classes) are registered in modules. Constructors receive dependencies. Scope: default (singleton), REQUEST (per-request), TRANSIENT (new each inject). Token-based injection with @Inject('TOKEN') allows non-class deps." },
  { t: "Node", d: "med", q: "How do you handle uncaught exceptions in Node?", a: "process.on('uncaughtException') and process.on('unhandledRejection') catch top-level errors. After logging, exit the process — state may be corrupt. Use a process manager (PM2, systemd) to restart. Better: wrap async code in try/catch and use global error middleware in your framework." },
  { t: "Node", d: "hard", q: "How do you scale a Node app on a multi-core machine?", a: "Use the cluster module or PM2 cluster mode to fork one process per core. Each worker has its own event loop. Share nothing — keep state in Redis/DB. For more, use multiple machines behind a load balancer. Node 21+ has built-in cluster improvements." },
  { t: "Node", d: "easy", q: "What does package-lock.json do?", a: "Locks the exact version of every transitive dependency. Without it, npm install on different machines could install different versions. Always commit it. Use npm ci in CI for deterministic installs from lock." },
  { t: "Node", d: "med", q: "What is the difference between cookies and JWT for auth?", a: "Cookies are stored by the browser and sent automatically with each request. JWT is a stateless token usually sent in Authorization header. Cookies need CSRF protection; JWT in localStorage needs XSS protection. Best practice: JWT as httpOnly cookie — combines benefits." },
  { t: "Node", d: "med", q: "How would you implement rate limiting in an Express/Nest app?", a: "Use express-rate-limit or nestjs/throttler. Backed by Redis for multi-instance correctness — use INCR with EXPIRE or sorted sets for sliding windows. Key by IP, user, or API key. Return 429 with Retry-After header." },
  { t: "Node", d: "med", q: "NestJS Guards vs Interceptors vs Pipes vs Filters", a: "Pipes transform/validate inputs before the handler. Guards make auth decisions (allow/deny). Interceptors wrap the handler — pre/post logic, transform response. Filters catch exceptions and format error responses. Execution order: Guards → Pipes → Handler → Interceptors → Filters." },
  { t: "Node", d: "med", q: "What are decorators in NestJS?", a: "TypeScript decorators (@Decorator) attach metadata. Built-in: @Module, @Controller, @Get, @Inject, @UseGuards. Custom decorators wrap createParamDecorator or composition. They are syntactic sugar over reflect-metadata." },
  { t: "Node", d: "hard", q: "How does Node handle file I/O internally?", a: "libuv uses a thread pool (default size 4, configurable via UV_THREADPOOL_SIZE) for fs operations, DNS lookups, and some crypto. Network I/O is handled by the OS event mechanism (epoll/kqueue/IOCP). So Node is single-threaded for JS but multi-threaded for I/O." },
  { t: "Node", d: "easy", q: "How do you debug a Node app?", a: "node --inspect or --inspect-brk to open V8 debugger on port 9229. Connect with Chrome DevTools (chrome://inspect) or VS Code. For prod: use heap snapshots, profiling via pprof equivalents, structured logging. Always have request_id propagation." },
  // ----- POSTGRESQL (20) -----
  { t: "SQL", d: "easy", q: "What is the difference between INNER JOIN and LEFT JOIN?", a: "INNER JOIN returns only rows present in both tables based on the join condition. LEFT JOIN returns all rows from the left table, with NULL for unmatched right-side columns. Useful for 'users and their optional profile' scenarios." },
  { t: "SQL", d: "med", q: "What is an index and what types does PostgreSQL support?", a: "An index is a separate data structure that speeds up lookups. Types: B-tree (default, equality + range), Hash (equality only), GIN (full-text, JSONB, arrays), GiST (geospatial, ranges), BRIN (huge sequential tables), SP-GiST. Partial indexes only index rows matching a condition. Multi-column composite indexes follow leftmost-prefix rule." },
  { t: "SQL", d: "med", q: "Explain the N+1 query problem", a: "Loading a list, then issuing one query per item. Example: fetching 100 users, then 100 queries for orders. Fix: single query with JOIN, or two queries — one for users, one for orders WHERE user_id IN (...). ORMs often hide this — watch for it in code review." },
  { t: "SQL", d: "med", q: "What does EXPLAIN ANALYZE tell you?", a: "EXPLAIN shows the planned query plan. ANALYZE actually runs it and shows actual times, row counts, and buffers. Look for Seq Scan on large tables (missing index), discrepancies between estimated and actual rows (stale stats), and high disk reads (low cache hit)." },
  { t: "SQL", d: "med", q: "What is a transaction and what are ACID properties?", a: "A transaction is a unit of work that succeeds or fails atomically. ACID: Atomicity (all-or-nothing), Consistency (DB invariants preserved), Isolation (concurrent txns appear serial), Durability (committed data survives crash). BEGIN; ... COMMIT; or ROLLBACK;" },
  { t: "SQL", d: "hard", q: "Explain isolation levels with phantom reads", a: "Read Uncommitted: dirty reads possible (not in PG). Read Committed (PG default): no dirty, but non-repeatable and phantoms possible. Repeatable Read: no non-repeatable, in PG also no phantoms (snapshot isolation). Serializable: full serial behavior, may abort with serialization errors." },
  { t: "SQL", d: "med", q: "When should you denormalize?", a: "When read patterns dominate and JOINs are expensive at scale. Trade extra storage and write complexity for read speed. Common: materialized views, computed columns, read-time tables for dashboards. Always have a script to rebuild from source of truth." },
  { t: "SQL", d: "med", q: "What is a CTE and when do you use it?", a: "Common Table Expression — a named subquery defined with WITH. Improves readability. RECURSIVE CTEs handle hierarchies (org charts, comment trees). Note: in PG <12, CTEs were optimization fences; from PG 12 they can be inlined." },
  { t: "SQL", d: "hard", q: "How do window functions differ from GROUP BY?", a: "GROUP BY collapses rows into aggregates. Window functions compute over a window of rows but keep the original rows. Example: ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY date) gives each row a rank without losing data. Useful for top-N-per-group, running totals, lag/lead." },
  { t: "SQL", d: "med", q: "When would you use a partial index?", a: "When most queries target a specific subset. Example: CREATE INDEX idx_active_users ON users(email) WHERE status = 'active'. Smaller, faster to scan, faster to maintain. Common with soft-delete flags." },
  { t: "SQL", d: "med", q: "What is connection pooling and why use PgBouncer?", a: "Postgres allocates ~10MB per connection. With many app servers and short queries, connection overhead dominates. PgBouncer multiplexes thousands of client connections onto a small number of Postgres connections. Use transaction-pooling mode for most web apps. Note: prepared statements and some session features behave differently." },
  { t: "SQL", d: "hard", q: "How would you handle a high-write workload in Postgres?", a: "Batch inserts (one statement with many VALUES), use COPY for bulk loads, partition by time, raise checkpoint timeouts, tune wal_buffers and synchronous_commit (off for non-critical writes), use UNLOGGED tables for ephemeral data, add read replicas, consider partitioning or sharding." },
  { t: "SQL", d: "med", q: "What is the difference between TRUNCATE and DELETE?", a: "DELETE removes rows one by one, triggers fire, MVCC creates dead tuples (need vacuum). TRUNCATE drops the entire table content nearly instantly, resets sequences (with RESTART IDENTITY), but cannot be rolled back inside a transaction with snapshot isolation in some setups. Use TRUNCATE for fast empties of staging tables." },
  { t: "SQL", d: "hard", q: "How does Postgres MVCC work?", a: "Each row version has xmin (created) and xmax (deleted) transaction IDs. Readers see only the versions valid at their snapshot. No read locks needed. Cost: dead tuples accumulate; VACUUM cleans them up. Long-running txns block vacuum and inflate bloat." },
  { t: "SQL", d: "med", q: "When would you use JSONB vs JSON in Postgres?", a: "JSONB stores parsed binary form — faster to query, supports indexing with GIN, slightly slower to insert. JSON stores the original text. Use JSONB 99% of the time. Use JSON only if you need to preserve formatting exactly." },
  { t: "SQL", d: "med", q: "What is UPSERT and how do you do it in Postgres?", a: "INSERT ... ON CONFLICT (col) DO UPDATE SET ... WHERE ...; or DO NOTHING. Atomically insert or update based on a unique constraint. Common for sync workflows. Watch for serialization conflicts under high concurrency." },
  { t: "SQL", d: "hard", q: "Explain the difference between SERIAL and IDENTITY columns", a: "SERIAL is the legacy approach using a sequence behind the scenes. IDENTITY (SQL standard, PG 10+) is cleaner: GENERATED ALWAYS AS IDENTITY. Both auto-increment. Prefer IDENTITY for new schemas. For distributed IDs, use UUIDs or snowflake-style." },
  { t: "SQL", d: "med", q: "What is a deadlock and how does Postgres handle it?", a: "Two transactions hold locks the other needs, waiting forever. Postgres detects deadlocks automatically (default 1s timeout) and aborts the cheaper transaction with a serialization failure. App should retry. Prevent by acquiring locks in a consistent order across code paths." },
  { t: "SQL", d: "med", q: "How do you implement soft delete?", a: "Add a deleted_at TIMESTAMPTZ NULL column. Filter WHERE deleted_at IS NULL on reads. Create a partial unique index excluding deleted rows to allow reusing emails after deletion. Add a scheduled job to hard-delete old soft-deleted rows for compliance." },
  { t: "SQL", d: "hard", q: "When would you reach for a different DB instead of Postgres?", a: "Postgres handles 90% of use cases. Reach for: ClickHouse/BigQuery for OLAP at billion-row scale, Cassandra/DynamoDB for global write-heavy KV with low latency, Elasticsearch/OpenSearch for full-text + faceted search, Redis for sub-ms hot data, Neo4j for deep graph traversals." },
  // ----- REDIS (10) -----
  { t: "Redis", d: "easy", q: "What is Redis primarily used for?", a: "In-memory key-value data store. Common uses: cache, session store, queues (List/Stream), pub/sub, rate limiting, leaderboards (Sorted Set), distributed locks. Single-threaded for commands (predictable, no race in single op), persistence optional." },
  { t: "Redis", d: "med", q: "Explain cache-aside, write-through, and write-behind", a: "Cache-aside: app reads cache; on miss, reads DB and writes back. Most common. Write-through: app writes both cache and DB synchronously — consistent, slower writes. Write-behind: write to cache, async to DB — fast writes, risk of data loss." },
  { t: "Redis", d: "med", q: "What is a thundering herd and how do you prevent it?", a: "Many requests hit a cold cache simultaneously and all stampede the DB. Prevent with: single-flight (only one regenerates while others wait), refresh-ahead (refresh before expiry), or jittered TTLs so expirations are not clustered." },
  { t: "Redis", d: "med", q: "What eviction policies does Redis offer?", a: "noeviction (default, reject writes), allkeys-lru, allkeys-lfu, allkeys-random, volatile-lru (only keys with TTL), volatile-lfu, volatile-random, volatile-ttl (closest to expiry first). For caches use allkeys-lru or allkeys-lfu. For queues, noeviction with monitoring." },
  { t: "Redis", d: "med", q: "How would you implement a rate limiter with Redis?", a: "Token bucket: INCR key with EXPIRE on first set, check if count > limit, return 429. For sliding window: store request timestamps in a Sorted Set, ZREMRANGEBYSCORE to drop old, ZCARD to count. Use Lua for atomicity across multiple commands." },
  { t: "Redis", d: "easy", q: "What data structures does Redis support?", a: "String, List, Hash, Set, Sorted Set, Stream, HyperLogLog, Bitmap, Geo, Bitfield. Each maps to common use cases: Hash for objects, Sorted Set for leaderboards, Stream for event logs with consumer groups." },
  { t: "Redis", d: "hard", q: "How does Redis persistence work?", a: "RDB: periodic point-in-time snapshots, fast restart, may lose data since last snapshot. AOF: append-only log of every write, configurable fsync (always/everysec/no), more durable, larger files. Most production setups enable both. Tune save and appendfsync to your durability needs." },
  { t: "Redis", d: "hard", q: "How would you use Redis for distributed locking?", a: "Simple: SET key uniqueval NX EX 30 to acquire. Release with a Lua script that DELs only if value matches. For higher correctness, Redlock algorithm across N independent masters — but read Martin Kleppmann critique before relying on it for safety-critical things." },
  { t: "Redis", d: "med", q: "How is Redis Cluster different from Redis Sentinel?", a: "Sentinel: high availability for single primary + replicas. Auto-failover, no sharding. Cluster: shards data across multiple primaries with automatic key partitioning (16384 hash slots) and replicas — both HA and horizontal scale. Cluster has some constraints: multi-key ops only within the same slot (use hash tags)." },
  { t: "Redis", d: "med", q: "What is a Redis pipeline and when do you use it?", a: "Send multiple commands without waiting for individual responses, then read all replies. Cuts network round-trips dramatically. Use for bulk operations. Not the same as MULTI/EXEC (transaction) — pipelines are not atomic, just batched." },
  // ----- DOCKER / DEVOPS (10) -----
  { t: "Docker", d: "easy", q: "What is the difference between an image and a container?", a: "An image is a read-only template — a layered filesystem with metadata. A container is a running instance of an image — image plus a writable layer. Many containers can run from one image." },
  { t: "Docker", d: "med", q: "Why use multi-stage builds?", a: "Build stages have build tools (compilers, dev deps), but the final image only needs the artifact. Multi-stage lets you copy only the binary into a tiny base image (scratch, alpine, distroless). Drastically reduces image size and attack surface — a 1GB Go build can become 15MB." },
  { t: "Docker", d: "med", q: "How do you reduce Docker image size?", a: "Use slim/alpine/distroless base, multi-stage builds, .dockerignore to skip cruft, combine RUN steps to reduce layers, remove apt cache (rm -rf /var/lib/apt/lists/*), use static binaries (CGO_ENABLED=0 for Go)." },
  { t: "Docker", d: "med", q: "What is the difference between CMD and ENTRYPOINT?", a: "ENTRYPOINT is the command always run. CMD provides default arguments. Together: ENTRYPOINT ['./app'] + CMD ['--prod'] runs ./app --prod, but docker run img --dev runs ./app --dev. Use ENTRYPOINT for the binary, CMD for default flags." },
  { t: "Docker", d: "med", q: "How does Docker networking work?", a: "Bridge (default): internal isolated network with NAT to host. Host: container shares host network. Overlay: multi-host (Swarm/k8s). Containers on the same user-defined network resolve each other by container name via embedded DNS." },
  { t: "Docker", d: "easy", q: "What is a volume vs a bind mount?", a: "Volume: managed by Docker (under /var/lib/docker/volumes/), best for databases. Bind mount: a host directory mapped into the container, good for local dev or sharing host files. tmpfs: in-memory only, for secrets." },
  { t: "Docker", d: "hard", q: "How would you structure a CI/CD pipeline?", a: "On PR: lint → unit tests → build image (cache from previous) → integration tests → vulnerability scan. On merge to main: tag image, push to registry, deploy to staging, run smoke tests, then promote to prod (manual or auto). Use feature flags for risky changes; blue-green or canary deploys." },
  { t: "Docker", d: "med", q: "What is a sidecar container?", a: "A helper container in the same Pod (k8s) sharing network and volumes — for logging, proxying (Envoy), config reload, secret rotation. Sidecar should not own primary business logic, just augment the main container." },
  { t: "Docker", d: "med", q: "How do you handle secrets in Docker?", a: "Never bake into the image. Options: env vars (visible in inspect), Docker secrets (Swarm), bind-mount files, or external KMS — AWS Secrets Manager / Vault. App reads at startup. For k8s use Secret resources backed by encrypted store, ideally with Workload Identity / IRSA so the pod fetches from cloud KMS." },
  { t: "Docker", d: "hard", q: "What is the difference between Docker Swarm and Kubernetes?", a: "Swarm: simpler, built into Docker, fine for small clusters. K8s: industry standard, much richer (CRDs, operators, autoscaling, service mesh), much steeper learning curve. Most companies past 10 services pick k8s." },
  // ----- MICROSERVICES / API (15) -----
  { t: "API", d: "easy", q: "What is REST and what are its principles?", a: "Representational State Transfer. Principles: stateless server, resource-based URLs (nouns), uniform interface (HTTP verbs), client-server separation, cacheable responses, layered system. HATEOAS is the strictest level, rarely seen in practice." },
  { t: "API", d: "easy", q: "What are common HTTP status codes you should know cold?", a: "200 OK, 201 Created, 204 No Content. 301/302 redirects. 400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found, 409 Conflict, 422 Unprocessable, 429 Rate Limited. 500 Internal Server Error, 502 Bad Gateway, 503 Service Unavailable, 504 Gateway Timeout." },
  { t: "API", d: "med", q: "How does JWT work?", a: "Token format: header.payload.signature, base64url encoded. Server signs payload (claims) with secret (HS256) or private key (RS256). Client sends in Authorization: Bearer <token>. Server verifies signature and expiry on each request. No DB lookup needed — stateless." },
  { t: "API", d: "med", q: "When should you use JWT in a cookie vs Authorization header?", a: "Cookie (httpOnly, SameSite=Lax/Strict, Secure) defends XSS theft but needs CSRF protection. Authorization header (token in JS memory) avoids CSRF but exposes to XSS. Best: short-lived access token in cookie, refresh token stored encrypted server-side or in a separate httpOnly cookie." },
  { t: "API", d: "med", q: "What is idempotency and why does it matter for APIs?", a: "An operation is idempotent if applying it multiple times has the same effect as once. GET, PUT, DELETE are idempotent; POST is not. For unsafe operations like payments, require an Idempotency-Key header so retries do not duplicate. Server stores the first response keyed by the idempotency key." },
  { t: "API", d: "med", q: "How do you version a REST API?", a: "URL versioning (/v1/...) is most common and visible. Header versioning (Accept: application/vnd.api+json;version=1) is cleaner but less discoverable. Add new fields without bumping version; bump for breaking changes. Have a deprecation policy: announce, sunset header, then remove." },
  { t: "API", d: "med", q: "When would you choose gRPC over REST?", a: "gRPC: lower latency, strong contracts (protobuf), bi-directional streaming, smaller payloads. Use for internal service-to-service. REST/JSON: better for public APIs, browser-friendly, easier debugging. GraphQL: client-driven queries, good for varied client needs." },
  { t: "API", d: "med", q: "How do you handle pagination?", a: "Offset/limit: simple but slow for deep pages (DB still scans skipped rows). Cursor-based: pass an opaque cursor (e.g., last ID or timestamp) — O(log n) regardless of depth. Keyset pagination is the technical term. Use cursor for infinite scroll, offset for fixed page UI." },
  { t: "API", d: "hard", q: "Design a webhook system", a: "Producer: emit event to queue. Worker: load subscriber config, sign payload (HMAC SHA256), POST with retry (exponential backoff up to e.g. 24h), log delivery state. Subscriber must respond 2xx within timeout (~5s). Provide replay endpoint and signing secret rotation. Always idempotent." },
  { t: "API", d: "med", q: "What is CORS and how do you handle it?", a: "Cross-Origin Resource Sharing — browser security that blocks JS from reading responses across origins unless server opts in via Access-Control-Allow-Origin headers. Preflight OPTIONS request for non-simple methods. Configure in middleware: allow specific origins (not *), credentials only with explicit origin." },
  { t: "API", d: "hard", q: "Microservices vs Modular Monolith — when to pick what?", a: "Monolith: fewer ops costs, ACID transactions across modules, easier debugging, fine until team coordination becomes the bottleneck. Microservices: independent deploys, polyglot, scale parts independently, but require investment in tracing/CI/contracts. Most teams should start modular monolith and split when justified." },
  { t: "API", d: "med", q: "What is a circuit breaker?", a: "Resilience pattern: after N failures, 'open' the circuit and fail fast for some duration without calling the downstream. Periodically half-open to test. Prevents cascading failures and gives the dependency time to recover. Implementations: Hystrix (deprecated), resilience4j (Java), gobreaker (Go)." },
  { t: "API", d: "med", q: "How do you implement health checks?", a: "/healthz returns 200 if process alive (liveness). /readyz returns 200 only when all critical deps (DB, cache) are reachable (readiness). K8s and load balancers use these to route traffic. Deep health checks separately — they should NOT fail readyz unless truly required for serving traffic." },
  { t: "API", d: "hard", q: "Saga pattern for distributed transactions", a: "Long-running multi-service operation broken into local transactions with compensating actions on failure. Choreography: each service emits events the next listens to. Orchestration: a central coordinator drives each step. Used for payments, order fulfillment, account opening." },
  { t: "API", d: "med", q: "What is the outbox pattern?", a: "Atomically write business state and an event row to the same DB transaction. A separate publisher process reads the outbox and publishes to Kafka/queue, then marks delivered. Solves the dual-write problem where DB write succeeds but message publish fails (or vice versa)." },
  // ----- NEXT.JS / REACT (15) -----
  { t: "Next/React", d: "easy", q: "What is the difference between SSR, SSG, and ISR in Next.js?", a: "SSR: HTML rendered on every request — fresh data, slower TTFB. SSG: HTML built at deploy — fastest, rebuild for updates. ISR: SSG + revalidate window — server regenerates page in the background after TTL. Pick per page based on freshness needs." },
  { t: "Next/React", d: "med", q: "What are React Server Components?", a: "Components that run only on the server, no client JS bundle. Can fetch data directly (await fetch). Cannot use state or browser APIs. Mark child components needing interactivity with 'use client'. Reduces bundle size and centralizes data fetching." },
  { t: "Next/React", d: "easy", q: "What is the virtual DOM?", a: "A lightweight in-memory tree representation of the UI. React diffs new vs old vDOM and applies minimal real DOM changes. Avoids expensive direct DOM manipulation. Note: vDOM is not always faster than well-written direct DOM code — its main value is the programming model." },
  { t: "Next/React", d: "med", q: "Explain useEffect dependencies", a: "Dependency array tells React when to re-run the effect. [] = once on mount. [x] = re-run when x changes. Missing deps cause stale closures (effect uses old values). Use the react-hooks/exhaustive-deps lint to catch this. For functions that should not retrigger, wrap in useCallback or move outside." },
  { t: "Next/React", d: "med", q: "When would you use useMemo vs useCallback?", a: "useMemo memoizes a computed value. useCallback memoizes a function reference. Both are about avoiding work — useMemo for expensive computations, useCallback to keep prop references stable so memoized children do not re-render. Do not wrap everything; profile first." },
  { t: "Next/React", d: "med", q: "What are React keys and why do they matter?", a: "Keys help React identify list items across renders. With stable, unique keys (NOT array index for reorderable lists), React preserves component state correctly. Using index keys on mutable lists causes weird state bugs — focus jumps, inputs hold wrong values." },
  { t: "Next/React", d: "easy", q: "What is hydration?", a: "Process where React attaches event listeners and reconciles server-rendered HTML with client React tree. Hydration errors happen when server and client render different HTML (Date.now, Math.random, conditional on window). Fix with useEffect for client-only logic." },
  { t: "Next/React", d: "med", q: "How does the Next.js App Router differ from Pages Router?", a: "App Router (13+): Server Components by default, nested layouts, route groups, streaming via Suspense, Server Actions. Pages Router: getServerSideProps/getStaticProps, _app and _document. App Router is the future, but Pages Router still receives critical updates." },
  { t: "Next/React", d: "med", q: "How would you handle global state in React?", a: "Tiers: useState (component-local), useContext (small global state, app-wide settings), Zustand or Jotai (medium, simple stores), Redux Toolkit (large, with devtools and middleware), Server state — React Query or SWR (cache, revalidate, mutations). Avoid putting server data in context." },
  { t: "Next/React", d: "hard", q: "How would you optimize a slow React page?", a: "Profile with React DevTools first. Then: memoize expensive children, virtualize long lists (react-window), code split with next/dynamic, use Server Components if possible, lazy-load images and below-the-fold, debounce inputs, batch state updates (React 18 auto-batches), preload data in route loaders." },
  { t: "Next/React", d: "med", q: "What are Server Actions in Next.js?", a: "Functions marked with 'use server' that execute on the server but can be called from client components — typically from form actions or onClick. No need to write API endpoints for mutations. Returns serialized data. Built on RPC over POST." },
  { t: "Next/React", d: "med", q: "When should you use SWR vs React Query?", a: "Both are excellent. SWR is lighter, simpler API, made by Vercel (Next.js team). React Query is richer — mutations, optimistic updates, query invalidation, devtools. For complex apps with lots of mutations, React Query. For simple read-heavy, SWR." },
  { t: "Next/React", d: "hard", q: "Explain reconciliation and the diffing algorithm", a: "React compares two trees and computes the minimum operations to convert old to new. Heuristics: different element types unmount the subtree, same type reuses. Keys disambiguate list items. The fiber architecture (React 16+) lets it pause/resume work for concurrency." },
  { t: "Next/React", d: "med", q: "What is hydration mismatch and how do you debug it?", a: "Server-rendered HTML differs from client first render. React logs a warning (or error in newer versions). Causes: random/date values, browser-only checks, conditional render on viewport. Fix: useEffect to set client-only state, or use Suspense with a server-safe fallback." },
  { t: "Next/React", d: "med", q: "What are accessibility (a11y) basics in React?", a: "Use semantic HTML (button not div), aria-* attributes only when necessary, focus management on route change, keyboard navigation (tab order, escape to close), alt text on images, sufficient color contrast (WCAG AA 4.5:1). Test with axe DevTools and screen readers." },
  // ----- SYSTEM DESIGN (15) -----
  { t: "SysDesign", d: "med", q: "How would you design a URL shortener?", a: "POST /shorten returns short code. Strategies: base62 encode auto-increment ID, or generate random 7-char and check collisions. Store in PG/Dynamo. Cache hot lookups in Redis. CDN for redirects. Analytics: Kafka → ClickHouse. Custom domains, expiry, abuse detection." },
  { t: "SysDesign", d: "med", q: "Design a rate limiter", a: "Token bucket per user+endpoint in Redis. INCR counter with EXPIRE, reject when over limit. For sliding window: sorted set of timestamps. Distribute via consistent hashing if scale demands. Provide Retry-After header. Allow burst with bucket size > rate." },
  { t: "SysDesign", d: "hard", q: "Design a notification system (your strong story!)", a: "Producers write to Kafka. Channel-specific consumers (email, SMS, push, WhatsApp) handle delivery, retries, idempotency keys, and provider rate limits. Template service merges user data. DLQ for permanent failures. User prefs in DB. Scheduling support. Audit log." },
  { t: "SysDesign", d: "med", q: "How would you design a leaderboard?", a: "Redis Sorted Set keyed by leaderboard ID. ZADD to update score, ZREVRANGE for top N, ZRANK for user rank. Sharding for global leaderboards by region or time. Persist final scores to DB on snapshot. Cache user position separately for fast personal view." },
  { t: "SysDesign", d: "hard", q: "Design a chat application like WhatsApp", a: "WebSocket gateways (stateful, sticky LB). Message store: Cassandra for write-heavy. Presence in Redis with heartbeat TTL. Fan-out via Redis Pub/Sub for online users; push notifications for offline. Media via S3 + CDN. E2EE handled client-side. Group chats with fan-out lists." },
  { t: "SysDesign", d: "med", q: "How do you design for high read availability?", a: "Read replicas behind a read endpoint, app routes reads to replica pool. Cache aggressively at multiple layers (CDN, app, DB). Stale-while-revalidate for soft freshness. Multi-region with eventual consistency for global. Health-check based failover." },
  { t: "SysDesign", d: "med", q: "How do you handle a sudden 10x traffic spike?", a: "Short term: scale stateless services horizontally (auto-scaling group, k8s HPA), enable rate limiting, shed non-critical work (queue lower priority items, disable heavy features). Cache more aggressively. Long term: capacity planning, load testing, async fallbacks for non-critical paths." },
  { t: "SysDesign", d: "hard", q: "Design a payment system at high level", a: "API layer (idempotency), ledger service (double-entry, immutable append), provider adapters (Stripe, payment gateway), webhook handler (HMAC verify, async process), reconciliation worker (nightly match), refund flow (reverse entries), audit log. Money never in floats. PCI-DSS via tokenization vault." },
  { t: "SysDesign", d: "med", q: "How would you store and query time-series data?", a: "Postgres with partitioning by time is fine to moderate scale. Past that: TimescaleDB (PG extension), InfluxDB, or ClickHouse for analytics. Use time-bucketed compaction, downsample old data, drop oldest partitions for retention." },
  { t: "SysDesign", d: "hard", q: "Design a feed system (Twitter/Instagram)", a: "Pull model: compute on read by joining followed users events — easy but slow for active users. Push model: fan-out on write — fast read, expensive for celebrities. Hybrid: push for normal users, pull for high-followers. Cache user feed in Redis (sorted set), backfill on access." },
  { t: "SysDesign", d: "med", q: "How do you ensure exactly-once delivery in a queue?", a: "Generally impossible at the protocol level. Achieved via at-least-once + idempotent consumers — use idempotency keys, dedup table, or check-then-act inside a transaction. Kafka has effectively-once with transactions across topics/consumers." },
  { t: "SysDesign", d: "med", q: "What is consistent hashing and where is it used?", a: "Distributes keys across a ring of nodes. Adding/removing a node only remaps ~1/N of keys (vs full reshuffle with mod-N). Virtual nodes per physical node balance load. Used in Cassandra, DynamoDB partitioning, Memcached client libraries, CDN edge selection." },
  { t: "SysDesign", d: "hard", q: "How would you build a search service?", a: "Source of truth in primary DB. Indexer service consumes change events (CDC, Debezium) and writes to Elasticsearch/OpenSearch with proper analyzers (lowercase, ngrams, stemming). Application calls search service which queries ES with filters/facets. Reindex pipeline for schema changes. Result re-ranking via business rules or ML." },
  { t: "SysDesign", d: "med", q: "What is CDN and what should you put on it?", a: "Content Delivery Network — edge caches close to users. Static assets (images, JS, CSS), generated HTML for SSG/ISR pages, public API responses with proper Cache-Control. Use Cache-Control: public, max-age=..., stale-while-revalidate. Purge on deploys." },
  { t: "SysDesign", d: "hard", q: "How do you design a multi-tenant SaaS?", a: "Tenancy levels: shared schema + tenant_id column (cheapest, isolation only logical), schema-per-tenant (medium), DB-per-tenant (strongest isolation, ops cost). Use row-level security if shared schema. Encrypt per-tenant keys for sensitive data. Background jobs and metrics must include tenant context." },
  // ----- BEHAVIORAL (15) -----
  { t: "Behavioral", d: "easy", q: "Tell me about yourself", a: "Use the 60s script: name, current role, 1–2 highlights with metrics (40% latency reduction), 1 thing you are looking for next. End warmly, invite questions." },
  { t: "Behavioral", d: "easy", q: "Walk me through your resume", a: "Reverse chronological. Spend 70% on most recent role (1Finance). Group earlier stints briefly. Highlight one technical win per role." },
  { t: "Behavioral", d: "med", q: "Why are you looking to switch?", a: "Positive framing: growth, scope, tech stack. 'I have learned a lot at 1Finance, especially on technical depth. I am looking for a role with more Go-native engineering culture and the chance to own larger systems end-to-end with stronger frontend scope.' Never criticize current team." },
  { t: "Behavioral", d: "med", q: "Tell me about a time you optimized a system", a: "Use Story 1 — the 40% latency reduction. Be specific about the diagnosis (EXPLAIN ANALYZE), action (composite indexes + Redis), and result (latency, throughput, zero downtime)." },
  { t: "Behavioral", d: "med", q: "Describe a difficult bug you fixed", a: "Pick the Redis TTL miss postmortem or the WordPress migration recovery. Show diagnosis process, the fix, and what you put in place to prevent it." },
  { t: "Behavioral", d: "med", q: "Tell me about a time you disagreed with a teammate", a: "Use Story 5 — scoring engine as stored procedure. Show that you brought data to the disagreement (prototyped both) rather than just opinion. Always end with what you learned from the other side." },
  { t: "Behavioral", d: "med", q: "How do you handle ambiguous requirements?", a: "Use Story 7 — the SEO audit tool. Steps you took: interviewed users, researched standards, distilled into concrete rules, validated incrementally." },
  { t: "Behavioral", d: "med", q: "Tell me about your biggest failure", a: "Use Story 8 — Redis TTL miss in production. Be specific. Do NOT pick a fake failure. End with what you put in place (CI check, code review checklist) so it never recurs." },
  { t: "Behavioral", d: "med", q: "How do you handle code review feedback?", a: "Treat it as data, not judgment. Ask for the reasoning when not obvious. Push back with rationale when I disagree, but commit to the team decision once made. I keep a personal note of common feedback patterns to internalize." },
  { t: "Behavioral", d: "med", q: "How do you mentor juniors?", a: "Use Story 6 — goroutine leaks. Pair on the live problem first (showing tools), then co-write checklist for repeatability, then push into team-wide CI rule for prevention. Multiply learning, do not just unblock." },
  { t: "Behavioral", d: "med", q: "How do you balance speed vs quality?", a: "Quality is non-negotiable for security, money, and data correctness paths. For everything else, ship MVP fast and iterate based on signal. I tend to bias toward shipping with strong observability so I can detect issues quickly even if I missed edge cases." },
  { t: "Behavioral", d: "med", q: "Where do you see yourself in 3 years?", a: "Senior engineer owning a core system end-to-end, mentoring 1–2 juniors, contributing to architectural direction. Ideally at a place where strong engineering culture lets me keep learning across both technical depth and frontend scope." },
  { t: "Behavioral", d: "med", q: "How do you keep learning?", a: "Mix of: deep reads (DDIA, Effective Go), engineering blogs (Stripe, Cred, Discord), side projects (real-time chat, India map), occasional courses (System Design Interview by ByteByteGo), and most importantly applying things in real work." },
  { t: "Behavioral", d: "med", q: "Describe a time you took initiative", a: "Use Story 3 — the blog platform PR/FAQ and Azure DevOps structured stories. Show you saw an unowned gap and stepped in with a concrete plan, not just a complaint." },
  { t: "Behavioral", d: "med", q: "What questions do you have for me?", a: "Always prepared 3–5. Examples: architecture overview, how technical decisions get made, what is a project they are proud of and what made it hard, success metrics for this role, on-call rotation, 30/60/90 day expectations." },
  // ----- FINTECH (10) -----
  { t: "Fintech", d: "med", q: "Why should money never be stored as a float?", a: "Floating point cannot represent decimals like 0.1 exactly. Sums and comparisons accumulate error. Use integer smallest-unit (paise/cents), or a Decimal type backed by arbitrary precision. Postgres NUMERIC(18,2). In Go: shopspring/decimal." },
  { t: "Fintech", d: "med", q: "What is double-entry accounting in software?", a: "Every transaction has two entries: a debit on one account and a credit on another. The two must balance. Ledger is immutable append-only — corrections are reversing entries. Balance is derived. This makes audits possible and prevents lost money." },
  { t: "Fintech", d: "med", q: "How do you handle idempotency for payments?", a: "Client sends Idempotency-Key header. Server stores key + first response. Subsequent same-key requests return cached response. TTL of 24h+. Prevents double-charging on retries, network blips, browser refreshes." },
  { t: "Fintech", d: "med", q: "How do you verify a payment webhook is authentic?", a: "Provider signs the body with a shared secret (HMAC-SHA256 typically). Recompute the signature on receipt and compare in constant time. Also check timestamp to prevent replay (reject if > 5 min old). Process idempotently — providers retry." },
  { t: "Fintech", d: "hard", q: "How would you reconcile an internal ledger with bank statements?", a: "Nightly job: ingest bank statement, match each line against internal txn by amount + reference. Discrepancies (timing, missing on either side) go to an exception queue for ops to resolve. Track aging — old breaks indicate process problems." },
  { t: "Fintech", d: "med", q: "What is PCI-DSS and when does it apply?", a: "Payment Card Industry Data Security Standard — required if you touch raw card numbers (PAN). Most teams stay out of scope by tokenizing through a vault (Stripe, payment gateway) — you store only opaque tokens. If you do touch PANs, expect heavy audit, network segmentation, encryption everywhere." },
  { t: "Fintech", d: "med", q: "How do you compute EMI?", a: "EMI = P × r × (1+r)^n / ((1+r)^n − 1) where P=principal, r=monthly rate (annual/12/100), n=months. Amortization schedule iterates: interest = balance × r, principal = EMI − interest, balance reduces. Always test with edge cases (r=0, very small r)." },
  { t: "Fintech", d: "med", q: "How would you secure PII in your DB?", a: "Encrypt at rest (column-level for sensitive fields like Aadhaar/PAN, full-disk for everything). Mask in logs (never log full PII). Role-based access. Audit who reads what. Pseudonymize in non-prod (synthetic test data). Hash + salt for any deterministic lookup." },
  { t: "Fintech", d: "med", q: "What is KYC and AML?", a: "KYC = Know Your Customer — verify identity (PAN, Aadhaar, address proof) before onboarding. AML = Anti-Money Laundering — monitor transactions for suspicious patterns, report to regulators. In India, governed by RBI and FIU-IND. Engineering side: integrate with bureaus (Hyperverge, IDfy) and risk engines." },
  { t: "Fintech", d: "hard", q: "How would you design a wallet with frozen/locked balance?", a: "Per-account: total = available + frozen. Lock operation: move amount from available to frozen, return a lock_id. Settle: charge against the lock (frozen decreases, total decreases). Release: unfreeze back to available. All operations inside a serializable transaction or with row-level locks to prevent race." },
  // ----- PHP -----
  { t: "PHP", d: "easy", q: "What is PHP? How is it executed?", a: "PHP is a server-side scripting language primarily used for web development. It is executed on the server, generating HTML which is then sent to the client. Common execution methods include PHP-FPM (FastCGI Process Manager) and Apache mod_php." },
  { t: "PHP", d: "easy", q: "What is the difference between == and === in PHP?", a: "== is loose equality (checks value after type coercion), while === is strict equality (checks both value and type without coercion). Always use === to avoid unexpected bugs, e.g., 0 == '0' is true, 0 === '0' is false." },
  { t: "PHP", d: "med", q: "What are magic methods in PHP? Give examples.", a: "Magic methods in PHP are special methods that override PHP's default action when certain actions are performed on an object. They start with __. Examples: __construct() (called on instantiation), __destruct() (called on destruction), __get(), __set() (overloading properties), __call() (overloading methods), __toString() (how the object reacts when treated as a string)." },
  { t: "PHP", d: "med", q: "How does PHP handle sessions?", a: "PHP uses session_start() to initialize or resume a session. It generates a unique Session ID, sends it to the client as a cookie (PHPSESSID), and creates a file on the server (or uses Redis/Memcached) to store the data linked to that ID. Access data via the $_SESSION superglobal array." },
  { t: "PHP", d: "med", q: "Explain Traits in PHP.", a: "Traits are a mechanism for code reuse in single inheritance languages like PHP. A trait is intended to reduce the limitations of single inheritance by enabling a developer to reuse sets of methods freely in several independent classes living in different class hierarchies." },
  { t: "PHP", d: "hard", q: "What are some new features introduced in PHP 8?", a: "JIT (Just-In-Time) compilation for performance, Union Types (e.g., int|float), Named Arguments, Match Expression (safer and cleaner alternative to switch), Nullsafe operator (?->), and Attributes (metadata for classes/methods instead of PHPDoc annotations)." },
  // ----- LARAVEL -----
  { t: "Laravel", d: "easy", q: "What is Laravel and what is artisan?", a: "Laravel is a free, open-source PHP web framework based on the MVC pattern. Artisan is the command-line interface included with Laravel, providing helpful commands to generate boilerplate code (controllers, models, migrations), run tasks, and manage database migrations." },
  { t: "Laravel", d: "med", q: "Explain Laravel's Service Container and Service Providers.", a: "The Service Container is an IoC (Inversion of Control) container for managing class dependencies and performing dependency injection. Service Providers are the central place of all Laravel application bootstrapping. They register bindings into the container, event listeners, middleware, and routes." },
  { t: "Laravel", d: "med", q: "Eloquent ORM vs Query Builder in Laravel?", a: "Eloquent is Laravel's Active Record implementation where each database table has a corresponding Model used to interact with that table. It is intuitive but can have an overhead. Query Builder provides a convenient, fluent interface to creating and running database queries, generally faster and better for complex/raw queries." },
  { t: "Laravel", d: "hard", q: "How do you handle queues and jobs in Laravel?", a: "Laravel provides a unified API across a variety of queue backends (Redis, Beanstalkd, SQS). You create Job classes (php artisan make:job). Jobs implement the ShouldQueue interface. You dispatch them using the dispatch() helper. Worker processes (php artisan queue:work) process the jobs in the background." },
  { t: "Laravel", d: "hard", q: "How does Dependency Injection work in Laravel?", a: "Laravel's Service Container automatically injects dependencies. If you type-hint a class in a Controller's constructor or method, Laravel uses Reflection to inspect the method signature, instantiate the required classes (and their dependencies recursively), and pass them to the method." },
  // ----- GO (Added Questions) -----
  { t: "Go", d: "easy", q: "How do you check if a key exists in a map in Go?", a: "Use the 'comma ok' idiom: val, ok := myMap[\"key\"]. If the key exists, ok is true and val is the value. If not, ok is false and val is the zero value for the map's value type." },
  { t: "Go", d: "easy", q: "How do you write a while loop in Go?", a: "Go only has the 'for' keyword. A while loop is just a for loop with a single condition: for i < 10 { // do something }." },
  { t: "Go", d: "hard", q: "How does the Go memory allocator work?", a: "It's based on TCMalloc (Thread-Caching Malloc). It uses mcache (per-P cache for lock-free small allocations), mcentral (global cache of spans), and mheap (manages all memory). It organizes memory into size classes (spans) to reduce fragmentation and allocation overhead." },
  { t: "Go", d: "hard", q: "Explain sync.Pool and when to use it.", a: "sync.Pool is a cache of allocated but unused items for later reuse, relieving pressure on the garbage collector. Items can be removed automatically during GC without notification. Great for short-lived, frequently allocated objects like byte buffers in high-throughput network servers (used extensively by fasthttp/Fiber)." },
  // ----- POSTGRESQL (Added Questions) -----
  { t: "SQL", d: "easy", q: "What is a Primary Key?", a: "A constraint that uniquely identifies each row in a table. It must contain UNIQUE values and cannot contain NULL values. A table can have only one primary key, which may consist of single or multiple columns (composite key)." },
  { t: "SQL", d: "easy", q: "What is the difference between WHERE and HAVING?", a: "WHERE filters rows BEFORE grouping or aggregation takes place. HAVING filters rows AFTER grouping or aggregation. You cannot use aggregate functions (like SUM or COUNT) in a WHERE clause, but you can in a HAVING clause." },
  { t: "SQL", d: "hard", q: "Explain table partitioning strategies in PostgreSQL.", a: "Partitioning splits large tables into smaller physical pieces while maintaining a single logical table. Strategies: RANGE (e.g., partitioning logs by month), LIST (e.g., by region or tenant ID), and HASH (distributing data evenly across partitions). Improves query performance (partition pruning) and maintenance (dropping old partitions is faster than DELETE)." },
  { t: "SQL", d: "hard", q: "Explain the difference between logical and physical replication.", a: "Physical replication (streaming replication) copies WAL files block-by-block, making an exact byte-for-byte copy (used for High Availability/Failover). Logical replication decodes WAL into DML statements (INSERT, UPDATE) and sends them to subscribers. Logical allows replicating specific tables, replicating across different PG versions, or to different databases." },
  // ----- MORE PHP -----
  { t: "PHP", d: "easy", q: "What are the main error types in PHP?", a: "Notices (non-critical, script continues), Warnings (more serious, script continues), and Fatal Errors (critical, script terminates). PHP 7+ also introduced Exceptions and Error classes that can be caught using try/catch." },
  { t: "PHP", d: "med", q: "How to handle exceptions in PHP?", a: "Use try-catch blocks. Code that might throw an exception goes in the try block, and catch blocks handle specific Exception or Error types. A finally block can be added for cleanup code that always runs regardless of an exception." },
  { t: "PHP", d: "med", q: "Explain PHP namespaces.", a: "Namespaces are used to avoid naming collisions between classes/functions/constants created by you and third-party libraries. They also provide the ability to alias (import) a long name to a shorter one, improving readability. Defined using the 'namespace' keyword at the top of the file." },
  { t: "PHP", d: "easy", q: "Difference between require, require_once, include, include_once.", a: "include issues a Warning on failure but continues execution. require issues a Fatal Error and stops execution. The _once variants check if the file has already been included/required and prevent it from being loaded multiple times, avoiding redeclaration errors." },
  { t: "PHP", d: "med", q: "What is the use of the static keyword in PHP?", a: "Inside a class, it defines static properties/methods that belong to the class itself, not instances. Inside a function, a static variable retains its value between multiple calls to that function." },
  { t: "PHP", d: "hard", q: "How does late static binding work?", a: "Late static binding (using the 'static::' keyword instead of 'self::') allows inherited classes to override static methods/properties and have the parent class resolve to the called class at runtime, rather than the class where the method was defined." },
  { t: "PHP", d: "med", q: "What are anonymous functions and closures in PHP?", a: "Anonymous functions (closures) are functions without a specified name. They are often used as callback parameters. In PHP, closures can inherit variables from the parent scope using the 'use' keyword." },
  { t: "PHP", d: "easy", q: "What is the spaceship operator (<=>)?", a: "Introduced in PHP 7, it compares two expressions. Returns -1 if the left is less, 0 if equal, and 1 if the left is greater. Commonly used in custom sorting functions (usort)." },
  { t: "PHP", d: "med", q: "Explain autoloading and PSR-4.", a: "Autoloading automatically includes class files when a class is instantiated, eliminating manual require statements. PSR-4 is the standard describing a specification for autoloading classes from file paths, mapping namespaces to directory structures. Handled natively by Composer." },
  { t: "PHP", d: "med", q: "How to prevent SQL injection in PHP?", a: "Always use Prepared Statements and Parameterized Queries via PDO or MySQLi. Never concatenate user input directly into a SQL string. Prepared statements separate the query structure from the data, neutralizing injected SQL commands." },
  { t: "PHP", d: "hard", q: "What are generators in PHP and why use them?", a: "Generators provide an easy way to implement simple iterators without the overhead or complexity of implementing a class that implements the Iterator interface. They use the 'yield' keyword and are highly memory-efficient for iterating over large datasets." },
  { t: "PHP", d: "med", q: "Explain array_map vs array_filter.", a: "array_map applies a callback function to each element of an array, returning an array of the modified values. array_filter iterates over an array and passes each value to a callback; if the callback returns true, the element is kept in the resulting array." },
  { t: "PHP", d: "med", q: "How do you implement interfaces and abstract classes in PHP?", a: "An interface defines a contract of public methods a class must implement (using 'implements'). An abstract class can have both abstract methods (without body) and implemented methods (using 'extends'). A class can implement multiple interfaces but extend only one abstract class." },
  { t: "PHP", d: "easy", q: "What is the $GLOBALS superglobal?", a: "$GLOBALS is an associative array containing references to all variables which are currently defined in the global scope of the script. The variable names are the keys of the array." },
  // ----- MORE LARAVEL -----
  { t: "Laravel", d: "easy", q: "Explain the MVC architecture in Laravel.", a: "Model represents data and business logic (Eloquent). View handles the presentation layer (Blade templates). Controller routes user requests, manipulates the model, and passes data to the view." },
  { t: "Laravel", d: "easy", q: "What are Route parameters and Named Routes?", a: "Route parameters allow capturing segments of the URI (e.g., /user/{id}). Named routes allow generating URLs or redirects to specific routes using a given name via the route('name') helper, making URL changes easier to manage." },
  { t: "Laravel", d: "med", q: "Explain Route Model Binding.", a: "Route model binding provides a convenient way to automatically inject model instances directly into your routes or controllers based on the ID or a specific field in the URI, e.g., passing User $user automatically fetches the user by the ID in the URL." },
  { t: "Laravel", d: "med", q: "What is a Middleware and how to create one?", a: "Middleware provides a mechanism for inspecting and filtering HTTP requests entering your application (e.g., authentication, logging). Created via 'php artisan make:middleware'. They run before or after the controller logic." },
  { t: "Laravel", d: "easy", q: "Explain CSRF protection in Laravel.", a: "Laravel automatically generates a CSRF 'token' for each active user session. This token is used to verify that the authenticated user is the one actually making the requests. Used in forms via the @csrf Blade directive." },
  { t: "Laravel", d: "med", q: "How does Validation work in Laravel (FormRequests)?", a: "Laravel offers validation rules. FormRequests are custom request classes that encapsulate validation and authorization logic, keeping controllers clean. Created via 'php artisan make:request'. If validation fails, it automatically redirects back with errors." },
  { t: "Laravel", d: "hard", q: "Explain the Concept of Facades in Laravel.", a: "Facades provide a 'static' interface to classes that are available in the application's service container. They serve as proxies to underlying classes, providing a terse, memorable syntax while maintaining more testability and flexibility than traditional static methods." },
  { t: "Laravel", d: "easy", q: "What are Blade directives?", a: "Blade is Laravel's templating engine. Directives are shortcuts for PHP code, starting with @. Common ones include @if, @foreach, @extends, @section, and @yield for building layouts and control structures." },
  { t: "Laravel", d: "med", q: "Explain Eloquent Mutators and Accessors.", a: "Accessors format Eloquent attribute values when you retrieve them (e.g., capitalizing a name). Mutators format the value before saving it to the database (e.g., hashing a password). In modern Laravel, defined via a single method returning an Attribute instance." },
  { t: "Laravel", d: "med", q: "How to handle Many-to-Many relationships in Eloquent?", a: "Defined using the belongsToMany method on both models. It requires an intermediate 'pivot' table. You can retrieve pivot table data using the 'pivot' attribute on the models, and attach/detach related records easily." },
  { t: "Laravel", d: "med", q: "What is Eager Loading in Laravel and why use it?", a: "Eager loading alleviates the N+1 query problem. Instead of querying relationships lazily in a loop, you use the 'with()' method to load them in a couple of queries upfront (e.g., Post::with('comments')->get())." },
  { t: "Laravel", d: "easy", q: "How do you run database migrations and seeders?", a: "Migrations are run with 'php artisan migrate' to create/modify tables. Seeders populate tables with dummy data using 'php artisan db:seed'. Both can be combined: 'php artisan migrate --seed'." },
  { t: "Laravel", d: "med", q: "How to use Events and Listeners in Laravel?", a: "Events represent something that happened (e.g., UserRegistered). Listeners respond to the event (e.g., SendWelcomeEmail). They decouple logic. Dispatched via the event() helper. Listeners can be queued for background processing." },
  { t: "Laravel", d: "med", q: "Explain Task Scheduling in Laravel.", a: "Laravel's command scheduler allows you to fluently and expressively define your command schedule within the app/Console/Kernel.php file. It requires only a single cron entry on your server that calls 'php artisan schedule:run' every minute." },
  { t: "Laravel", d: "hard", q: "Explain the request lifecycle in Laravel.", a: "Public/index.php -> HTTP Kernel -> Global Middleware -> Router -> Route Middleware -> Controller -> View. Along the way, Service Providers bootstrap core components and bindings." },
  // ----- MORE GO -----
  { t: "Go", d: "easy", q: "What is the init() function and when is it executed?", a: "The init() function takes no arguments and returns no values. It runs automatically before the main() function, after all variable declarations in the package have evaluated their initializers. Used for setup tasks." },
  { t: "Go", d: "med", q: "How do you handle race conditions in Go?", a: "Race conditions occur when multiple goroutines access shared memory concurrently without synchronization. Detect them using the race detector during tests or builds: 'go test -race' or 'go build -race'. Fix them using Mutexes or Channels." },
  { t: "Go", d: "med", q: "Explain how method receivers work (value vs pointer).", a: "A value receiver gets a copy of the struct; modifications inside the method aren't reflected in the original. A pointer receiver gets the memory address, allowing the method to modify the original struct. Pointer receivers are also used to avoid copying large structs." },
  { t: "Go", d: "med", q: "What are Go build constraints/tags?", a: "Build tags (//go:build tagname) at the top of a file dictate under what conditions (OS, architecture, custom tags) the file should be included in the compilation. Useful for OS-specific implementations." },
  { t: "Go", d: "med", q: "How do you use the embed package in Go?", a: "The 'embed' package allows you to embed static files and folders directly into the Go binary at compile time. Use the //go:embed directive above a variable of type string, []byte, or embed.FS." },
  { t: "Go", d: "hard", q: "What is a memory leak in Go, given it's garbage collected?", a: "Memory leaks in Go happen when references to objects are unintentionally held, preventing the GC from reclaiming them. Common causes: appending to global slices indefinitely, unclosed response bodies, forgotten goroutines (goroutine leaks), and lingering references in maps." },
  { t: "Go", d: "easy", q: "How do you do cross-compilation in Go?", a: "Go natively supports cross-compilation. Just set the GOOS and GOARCH environment variables before running the build command. E.g., GOOS=linux GOARCH=amd64 go build -o myapp." },
  { t: "Go", d: "med", q: "What are the differences between log.Fatal and panic?", a: "log.Fatal prints a message and calls os.Exit(1), bypassing any deferred functions. panic initiates a normal panic sequence, executing deferred functions, which allows for recovery via the 'recover' function. Prefer returning errors, use panic only for unrecoverable state." },
  { t: "Go", d: "med", q: "Explain how type switches and type assertions work.", a: "Type assertion extracts the underlying concrete value of an interface: val := i.(string). Type switch is a construct that permits several type assertions in series: switch v := i.(type) { case int: ... case string: ... }." },
  { t: "Go", d: "med", q: "How do you mock interfaces in Go testing?", a: "Since interfaces are satisfied implicitly, you simply create a test struct that implements the interface's methods. Frameworks like gomock or mockery can auto-generate these mock implementations for large interfaces." },
  { t: "Go", d: "hard", q: "Explain the sync/atomic package and its use cases.", a: "The sync/atomic package provides low-level atomic memory primitives useful for implementing synchronization algorithms. It's used for simple counters or state flags without the overhead of a full Mutex, ensuring safe concurrent access." },
  { t: "Go", d: "med", q: "How do you read a large file chunk by chunk in Go?", a: "Instead of os.ReadFile (loads entirely into memory), use os.Open to get an *os.File, then wrap it in a bufio.Reader. Read in chunks using Read() into a pre-allocated byte slice, or line-by-line using ReadString('\\n') or a bufio.Scanner." },
  { t: "Go", d: "med", q: "What is the net/http ServeMux?", a: "ServeMux is an HTTP request multiplexer. It matches the URL of each incoming request against a list of registered patterns and calls the handler for the pattern that most closely matches the URL." },
  { t: "Go", d: "med", q: "How does Go handle dependency injection?", a: "Go does not have built-in DI frameworks like Spring. DI in Go is usually done manually via constructors: func NewService(db *sql.DB) *Service. Alternatively, libraries like google/wire use code generation for compile-time DI." },
  { t: "Go", d: "med", q: "What is go.work (Go workspaces)?", a: "Introduced in Go 1.18, go.work allows developers to work with multiple modules locally simultaneously without needing to manually modify go.mod files with 'replace' directives. It sets up a workspace spanning multiple modules." },
  { t: "Go", d: "hard", q: "Explain sync.Cond and when to use it.", a: "sync.Cond implements a condition variable, a rendezvous point for goroutines waiting for or announcing the occurrence of an event. Use it over channels when you need to broadcast a signal to *multiple* waiting goroutines simultaneously (using cond.Broadcast())." },
  // ----- MORE POSTGRESQL -----
  { t: "SQL", d: "easy", q: "What is a Foreign Key constraint?", a: "A Foreign Key is a column or group of columns in a relational database table that provides a link between data in two tables. It enforces referential integrity, ensuring the value matches a Primary Key in another table." },
  { t: "SQL", d: "easy", q: "Explain the difference between UNION and UNION ALL.", a: "Both combine the result sets of two or more SELECT statements. UNION removes duplicate rows from the combined result set, which incurs a performance cost for sorting/deduplication. UNION ALL keeps all duplicates and is significantly faster." },
  { t: "SQL", d: "med", q: "What is a Materialized View?", a: "Unlike a standard View which runs its underlying query every time it's accessed, a Materialized View stores the query result physically on disk. It speeds up read-heavy, complex queries but requires manual or scheduled refreshes (REFRESH MATERIALIZED VIEW) to reflect underlying data changes." },
  { t: "SQL", d: "med", q: "How do you handle pagination efficiently in PostgreSQL?", a: "OFFSET/LIMIT becomes very slow for deep pages because the DB still scans and discards the offset rows. Cursor-based (keyset) pagination uses a WHERE clause on an indexed column (e.g., WHERE id > last_seen_id LIMIT 10), which is fast regardless of depth." },
  { t: "SQL", d: "med", q: "What are Triggers in PostgreSQL?", a: "A trigger is a specification that the database should automatically execute a particular function whenever a certain type of operation (INSERT, UPDATE, DELETE) is performed on a specific table or view. Useful for audit trails or complex validations." },
  { t: "SQL", d: "med", q: "Explain Stored Procedures vs Functions in PostgreSQL.", a: "Functions return a value and cannot execute transaction control commands (COMMIT, ROLLBACK) inside them. Stored Procedures (introduced in PG 11) don't return a value natively but *can* manage transactions, making them suitable for complex batch jobs." },
  { t: "SQL", d: "easy", q: "What is a sequence in PostgreSQL?", a: "A sequence is a special kind of database object designed for generating unique, strictly ascending or descending numeric identifiers. Behind the scenes, SERIAL and IDENTITY columns use sequences." },
  { t: "SQL", d: "hard", q: "How does full-text search work in PostgreSQL?", a: "PG provides built-in full-text search. It parses documents into tokens (tsvector), reducing them to lexemes (e.g., 'running' -> 'run'). Queries are converted to tsquery. Using GIN indexes on tsvector columns enables highly efficient text searching without needing external engines like Elasticsearch." },
  { t: "SQL", d: "med", q: "What are dead tuples and how does VACUUM help?", a: "Because of MVCC, UPDATEs and DELETEs don't immediately remove old row versions; they become 'dead tuples'. These bloat the table and slow down scans. The VACUUM process reclaims the storage occupied by dead tuples so it can be reused by new inserts." },
  { t: "SQL", d: "med", q: "What is the pg_stat_statements extension?", a: "An official extension that records execution statistics of all SQL statements executed. It's the primary tool for identifying slow queries, high-CPU queries, and most frequently run queries in a production DB." },
  { t: "SQL", d: "med", q: "How to perform a cross join and when is it useful?", a: "A CROSS JOIN returns the Cartesian product of rows from tables in the join (every row in table A matched with every row in table B). Useful rarely, usually for generating test data matrices or pairing every user with every possible setting." },
  { t: "SQL", d: "easy", q: "What are temporary tables in PostgreSQL?", a: "Tables created with 'CREATE TEMP TABLE' are visible only to the current session and are automatically dropped when the session ends. Useful for storing intermediate results during complex calculations." },
  { t: "SQL", d: "hard", q: "Explain how to implement row-level security (RLS).", a: "RLS restricts which rows a database user can select, insert, update, or delete. You enable it with 'ALTER TABLE t ENABLE ROW LEVEL SECURITY', then create policies using 'CREATE POLICY' that define a boolean expression (e.g., checking if tenant_id matches the current user's setting) applied to every query." },
  { t: "SQL", d: "easy", q: "What is the RETURNING clause?", a: "In INSERT, UPDATE, or DELETE statements, the RETURNING clause allows you to return values from the modified rows (e.g., INSERT INTO users (name) VALUES ('x') RETURNING id;). Avoids needing a subsequent SELECT." },
  { t: "SQL", d: "hard", q: "How do you rename a column or table without downtime?", a: "You cannot simply RENAME in a high-traffic app. 1. Add the new column. 2. Update code to write to both old and new, and read from old. 3. Backfill old data to new column. 4. Update code to read from new. 5. Remove writes to old. 6. Drop old column." },
  { t: "SQL", d: "med", q: "What is a composite type in PostgreSQL?", a: "A composite type represents the structure of a row or record; it consists of a list of field names and their data types. You can create them explicitly via 'CREATE TYPE' and use them as column types, essentially storing rows within columns." }
];
